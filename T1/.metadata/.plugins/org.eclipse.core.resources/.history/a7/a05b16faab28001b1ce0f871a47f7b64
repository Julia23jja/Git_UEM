package ejer15;

import java.util.Scanner;

public class CompañiaTelefonica {
	static final double IMPUESTODOM = 0.3;
	static final double IMPUESTOMAN = 0.15;
	static final double IMPUESTOTAR = 0.10;

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		
		System.out.println("Introduce la duración de la llamada");
		double duracion = Double.parseDouble(sc.nextLine());
		
		System.out.println("Introduce el día que realizó la llamada");
		String dia = sc.nextLine();
		
		System.out.println("Introduce cuándo se realizó la llamada");
		String turno = sc.nextLine();
		
		double coste = 0;
		double impuesto;
		String domingo = null;
		String mañana = null;
		String tarde = null;
		
		if (duracion == 5) {
			coste = duracion * 1;
		} else if (duracion == 8) {
			coste = duracion * 0.8;
		} else if (duracion == 10) {
			coste = duracion * 0.70;
		} else if (duracion > 10) {
			coste = duracion * 0.50;
		}
		
		if (dia.equals(domingo)) {
			impuesto = coste * IMPUESTODOM;
		} else if (dia.equalsIgnoreCase(mañana)) {
			impuesto = coste * IMPUESTOMAN;
		} else if (dia.equalsIgnoreCase(tarde)) {
			impuesto = coste * IMPUESTOTAR;
		}
		
//SIN ACABAR
		
		sc.close();

	}

}
